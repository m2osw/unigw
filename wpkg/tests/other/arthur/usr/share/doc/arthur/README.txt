The sample4 test is expected to be created with the wpkg tool
with a command line similar to:

    wpkg --build sample4

The result is a debian package named sample4_3.0_win32-i386.deb
which can be installed to test the pre/post installation and
removal scripts.

